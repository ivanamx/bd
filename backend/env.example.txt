# Configuración de la Base de Datos PostgreSQL
# Copia este archivo y renómbralo a .env

DB_HOST=localhost
DB_PORT=5432
DB_NAME=lorei_encounters
DB_USER=postgres
DB_PASSWORD=tu_contraseña

# Puerto del servidor
PORT=8765

# Configuración de IA (Google Gemini)
# Para activar el análisis con IA real, descomenta y configura:
# GOOGLE_AI_API_KEY=tu-api-key-de-google-aqui
# GOOGLE_AI_MODEL=gemini-pro (o gemini-1.5-pro, gemini-1.5-flash)
# 
# Si no configuras estas variables, se usará el análisis básico (hardcoded)

